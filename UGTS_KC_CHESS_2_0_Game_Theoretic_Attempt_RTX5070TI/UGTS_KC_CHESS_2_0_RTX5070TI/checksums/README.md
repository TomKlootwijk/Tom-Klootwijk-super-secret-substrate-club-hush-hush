# Package checksums

`SHA256SUMS.txt` lists every delivered file except itself. Paths are relative to the package root.
`validation/file_manifest.json` is a machine-readable inventory and intentionally excludes itself and `checksums/SHA256SUMS.txt` to avoid recursive hashes.
